In terminal when in directory First-React-App, run "npm run dev" to begin the server running

You will need to install some other dependencies if you wish to run on your own local device these include:
1. react-router-dom
2. react-p5
3. gsap

   
